{
  "name": "@type-iclient/monorepo",
  "version": "0.4.0-alpha",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "bundle": "pnpm --stream --parallel --filter \"./packages/**\" bundle",
    "bundle:browser": "pnpm --stream --parallel --filter \"./packages/**\" bundle:browser",
    "build": "pnpm --stream --parallel --filter \"./packages/**\" build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.15.7",
    "typescript": "^4.8.2"
  },
  "dependencies": {
    "@type-iclient/iserver": "workspace:^*",
    "@type-iclient/ol": "workspace:^*"
  }
}
