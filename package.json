{
  "name": "@sm-iclient/monorepo",
  "version": "0.6.2-alpha",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "bundle": "pnpm --stream --parallel --filter \"./packages/**\" bundle",
    "bundle:browser": "pnpm --stream --parallel --filter \"./packages/**\" bundle:browser",
    "build": "pnpm --stream --parallel --filter \"./packages/**\" build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.15.7",
    "typescript": "^4.8.2"
  },
  "dependencies": {
    "@sm-iclient/iserver": "workspace:^*",
    "@sm-iclient/ol": "workspace:^*"
  }
}
